<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzy.mapper.ReadNoticeMapper">
    <insert id="addReadNotice" parameterType="ReadNotice">
        insert into readnotice(noticeId,userId,expirationTime)
        values (#{noticeId},#{userId},#{expirationTime})
    </insert>
</mapper>
